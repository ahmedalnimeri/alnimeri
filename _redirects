# Nothing here yet.
#
# www -> apex cannot be done from this file: Cloudflare Pages only matches
# path-relative sources in _redirects, not hostnames, and it serves an
# existing static file before consulting redirects at all. The fix is a
# dashboard Redirect Rule (Rules -> Redirect Rules) or removing www as a
# custom domain on the Pages project. Until then the canonical tag on every
# page names the apex, which is what search engines consolidate on.
